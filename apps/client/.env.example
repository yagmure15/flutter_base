# Copy this file to .env.dev / .env.staging / .env.prod and fill in real values.
# These files are git-ignored; the generated lib/env/*.g.dart files are too.
BASE_URL=https://dev.api.example.com
KEY=your_api_key
